/**  * Sistema centralizado de tratamento de erros  * Garante consist├¬ncia em toda a aplica├º├úo  */  export class AppError extends Error {   constructor(message, code = 'UNKNOWN_ERROR', statusCode = 500) {     super(message);     this.name = 'AppError';     this.code = code;     this.statusCode = statusCode;   } }  export class AuthError extends AppError {   constructor(message = 'Erro de autentica├º├úo') {     super(message, 'AUTH_ERROR', 401);     this.name = 'AuthError';   } }  export class ValidationError extends AppError {   constructor(message = 'Dados inv├ílidos', details = {}) {     super(message, 'VALIDATION_ERROR', 400);     this.name = 'ValidationError';     this.details = details;   } }  export class NetworkError extends AppError {   constructor(message = 'Erro de conex├úo') {     super(message, 'NETWORK_ERROR', 0);     this.name = 'NetworkError';   } }  /**  * Extrai mensagem de erro de diferentes fontes  */ export function extractErrorMessage(data) {   if (!data) return 'Erro desconhecido';    // FastAPI validation errors   if (data.detail) {     if (Array.isArray(data.detail)) {       return data.detail.map(d => d.msg || d).join(', ');     }     return data.detail;   }    // Custom message field   if (data.message) return data.message;    // Generic error field   if (data.error) return data.error;    return 'Erro na requisi├º├úo'; }  /**  * Logger seguro para debugging  */ export class Logger {   static levels = {     DEBUG: 0,     INFO: 1,     WARN: 2,     ERROR: 3   };    static currentLevel = (typeof window !== 'undefined' && window.location.hostname !== 'localhost' && window.location.hostname !== '127.0.0.1')     ? this.levels.WARN      : this.levels.DEBUG;    static debug(label, data) {     if (this.currentLevel <= this.levels.DEBUG) {     }   }    static info(label, data) {     if (this.currentLevel <= this.levels.INFO) {       console.info(`[INFO] ${label}`, data);     }   }    static warn(label, data) {     if (this.currentLevel <= this.levels.WARN) {     }   }    static error(label, error) {     if (this.currentLevel <= this.levels.ERROR) {     }   } }  export default { AppError, AuthError, ValidationError, NetworkError, Logger };