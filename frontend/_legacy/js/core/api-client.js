/**  * Cliente HTTP unificado para toda a aplica├º├úo  * Gerencia autentica├º├úo, tratamento de erros e retry autom├ítico  */  import { getState, logout } from "../store.js"; import { API_V1_URL } from "./config.js"; import { AuthError, NetworkError, extractErrorMessage, Logger } from "./error-handler.js";  // Configura├º├úo de retry const RETRY_CONFIG = {   maxRetries: 3,   retryDelay: 1000, // ms   retryableCodes: [408, 429, 500, 502, 503, 504] };  /**  * Cliente API com suporte a retry, tratamento de erro e timeout  */ class APIClient {   constructor() {     this.timeout = 30000; // 30s   }    getAuthHeader() {     const { token } = getState();     return token ? { Authorization: `Bearer ${token}` } : {};   }    /**    * Executa requisi├º├úo com retry autom├ítico    */   async request(path, options = {}, retryCount = 0) {     const { token } = getState();          if (!token && !path.includes('/auth/login') && !path.includes('/auth/register')) {       throw new AuthError('Token n├úo dispon├¡vel');     }      const headers = {       "Content-Type": "application/json",       ...this.getAuthHeader(),       ...(options.headers || {})     };      const controller = new AbortController();     const timeoutId = setTimeout(() => controller.abort(), this.timeout);      try {       const response = await fetch(`${API_V1_URL}${path}`, {         ...options,         headers,         signal: controller.signal       });        clearTimeout(timeoutId);        // Tratamento de autentica├º├úo expirada       if (response.status === 401) {         Logger.warn('API', 'Token expirado, fazendo logout');         logout();         throw new AuthError('Sua sess├úo expirou. Fa├ºa login novamente.');       }        // Tratamento de resposta n├úo-OK       if (!response.ok) {         const contentType = response.headers.get('content-type');         let errorData = {};                  try {           if (contentType?.includes('application/json')) {             errorData = await response.json();           } else {             errorData = { error: await response.text() };           }         } catch {           errorData = { error: `HTTP ${response.status}` };         }          const message = extractErrorMessage(errorData);                  // Retry autom├ítico para erros tempor├írios         if (RETRY_CONFIG.retryableCodes.includes(response.status) && retryCount < RETRY_CONFIG.maxRetries) {           Logger.warn('API', `Retry ${retryCount + 1}/${RETRY_CONFIG.maxRetries} para ${path}`);           await this.sleep(RETRY_CONFIG.retryDelay);           return this.request(path, options, retryCount + 1);         }          throw new Error(message);       }        // Parse resposta       const contentType = response.headers.get('content-type');       if (contentType?.includes('application/json')) {         return await response.json();       }              return null;     } catch (error) {       clearTimeout(timeoutId);        if (error.name === 'AbortError') {         throw new NetworkError('Timeout na requisi├º├úo. Tente novamente.');       }        throw error;     }   }    /**    * GET request    */   async get(path, options = {}) {     return this.request(path, { ...options, method: 'GET' });   }    /**    * POST request    */   async post(path, data = {}, options = {}) {     return this.request(path, {       ...options,       method: 'POST',       body: JSON.stringify(data)     });   }    /**    * PUT request    */   async put(path, data = {}, options = {}) {     return this.request(path, {       ...options,       method: 'PUT',       body: JSON.stringify(data)     });   }    /**    * DELETE request    */   async delete(path, options = {}) {     return this.request(path, { ...options, method: 'DELETE' });   }    /**    * Upload file    */   async uploadFile(path, file, additionalData = {}) {     const formData = new FormData();     formData.append('file', file);          Object.entries(additionalData).forEach(([key, value]) => {       formData.append(key, value);     });      const response = await fetch(`${API_V1_URL}${path}`, {       method: 'POST',       headers: this.getAuthHeader(),       body: formData     });      if (response.status === 401) {       logout();       throw new AuthError('Sua sess├úo expirou.');     }      if (!response.ok) {       const error = await response.json();       throw new Error(extractErrorMessage(error));     }      return response.json();   }    sleep(ms) {     return new Promise(resolve => setTimeout(resolve, ms));   } }  // Inst├óncia singleton const apiClient = new APIClient();  // Export para compatibilidade com c├│digo existente export { apiClient };  // Export fun├º├úo legacy para retrocompatibilidade export async function apiRequest(path, options = {}) {   return apiClient.request(path, options); }